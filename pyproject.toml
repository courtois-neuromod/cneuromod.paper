[project]
name = "cneuromod-paper"
version = "0.1.0"
description = "The Courtois NeuroMod (CNeuroMod) Dataset paper"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=7.2.0",
    "jupyter-book>=2.0",
    "matplotlib>=3.10.9",
]

[dependency-groups]
dev = []
